---
const stats = [
  { label: "Diện tích tự nhiên", value: "41,11 km²" },
  { label: "Dân số 2025", value: "30.864 người" },
  { label: "Mật độ", value: "751 người/km²" },
  { label: "Ngày hiệu lực", value: "01/07/2025" }
];

const mergedUnits = [
  "Tiến Xuân",
  "Thạch Hòa",
  "Cổ Đông (phần còn lại)",
  "Bình Yên (phần còn lại)",
  "Hạ Bằng (phần còn lại)",
  "Tân Xã (phần còn lại)"
];

const mapSrc = "https://www.openstreetmap.org/export/embed.html?bbox=105.44%2C20.97%2C105.59%2C21.06&layer=mapnik&marker=21.0119%2C105.514";
const adminMapSrc = "https://www.openstreetmap.org/export/embed.html?bbox=105.44%2C20.97%2C105.59%2C21.06&layer=transportmap";
---
<section class="geo">
  <div class="geo__inner">
    <header class="geo__head">
      <div>
        <p class="tag">Hành chính 2025</p>
        <h2>Bản đồ xã Hòa Lạc sau sáp nhập</h2>
        <p class="geo__lead">
          Một khối hành chính mới rộng 41,11 km² ôm trọn hành lang Đại Lộ Thăng Long, giữ vai trò vùng đệm chiến lược
          giữa khu Công nghệ cao và các đô thị vệ tinh phía Tây.
        </p>
      </div>
      <div class="geo__badge">
        <span>Update 11/2025</span>
      </div>
    </header>

    <div class="geo__map-hero">
      <iframe
        src={mapSrc}
        loading="lazy"
        referrerpolicy="no-referrer-when-downgrade"
        aria-label="Bản đồ xã Hòa Lạc"
      ></iframe>
      <div class="geo__map-pillars">
        <div>
          <p>Toạ độ trung tâm</p>
          <strong>21.0119°N • 105.514°E</strong>
        </div>
        <div>
          <p>Trụ sở</p>
          <strong>Thôn 1, Thạch Hòa</strong>
        </div>
        <div>
          <p>Portal</p>
          <strong><a href="https://hoalac.hanoi.gov.vn/" target="_blank" rel="noreferrer">hoalac.hanoi.gov.vn</a></strong>
        </div>
      </div>
      <p class="geo__source">Nguồn dữ liệu: TinhThanhVN 11/2025 · Map layer: OpenStreetMap</p>
    </div>

    <div class="geo__details">
      <div class="geo__stats">
        {stats.map((item) => (
          <article>
            <p class="geo__stat-label">{item.label}</p>
            <p class="geo__stat-value">{item.value}</p>
          </article>
        ))}
      </div>

      <div class="geo__admin">
        <div class="geo__admin-map">
          <iframe
            src={adminMapSrc}
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            aria-label="Bản đồ hành chính Hòa Lạc"
          ></iframe>
        </div>
        <div class="geo__admin-info">
          <h3>Bản đồ hành chính</h3>
          <p>
            Nghị quyết 202/2025/QH15 hợp nhất 6 xã: {mergedUnits.join(', ')}. Khối mới được quy hoạch cho
            nông nghiệp công nghệ cao, du lịch sinh thái và logistic hỗ trợ khu CNC.
          </p>
          <ul>
            <li>Trạng thái: hoạt động từ 01/07/2025</li>
            <li>Đơn vị quản lý: UBND xã Hòa Lạc</li>
            <li>Mã định danh: 09988</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .geo {
    margin: 0 auto 4rem;
    padding: 4rem 1.5rem;
    background: radial-gradient(circle at top right, rgba(29, 174, 152, 0.25), transparent 55%),
      radial-gradient(circle at bottom left, rgba(236, 118, 38, 0.2), transparent 45%),
      #f0f8f4;
  }
  .geo__inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }
  .geo__head {
    display: flex;
    justify-content: space-between;
    gap: 1.5rem;
    flex-wrap: wrap;
    align-items: center;
  }
  .geo__head h2 {
    margin: 0.4rem 0 0.6rem;
    font-size: clamp(2rem, 4vw, 2.8rem);
  }
  .geo__lead {
    margin: 0;
    max-width: 640px;
    color: var(--muted);
    font-size: 1.1rem;
  }
  .geo__badge {
    background: #0f172a;
    color: #fff;
    padding: 0.8rem 1.4rem;
    border-radius: 999px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .geo__map-hero {
    background: linear-gradient(135deg, rgba(15, 23, 42, 0.9), rgba(15, 23, 42, 0.7));
    border-radius: 1.75rem;
    padding: clamp(1.5rem, 3vw, 2.5rem);
    color: #fff;
    box-shadow: 0 25px 70px rgba(0, 0, 0, 0.25);
  }
  .geo__map-hero iframe {
    width: 100%;
    height: clamp(380px, 60vw, 540px);
    border: 0;
    border-radius: 1.25rem;
    margin-bottom: 1rem;
  }
  .geo__map-pillars {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1rem;
  }
  .geo__map-pillars div {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 1rem;
    padding: 1rem;
  }
  .geo__map-pillars p {
    margin: 0;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: rgba(255, 255, 255, 0.7);
  }
  .geo__map-pillars strong {
    display: block;
    margin-top: 0.4rem;
    font-size: 1.1rem;
  }
  .geo__map-pillars a {
    color: #fff;
    text-decoration: underline;
  }
  .geo__source {
    margin: 0.75rem 0 0;
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.75);
    text-align: right;
  }
  .geo__details {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .geo__stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 1rem;
  }
  .geo__stats article {
    background: #fff;
    padding: 1.2rem 1.4rem;
    border-radius: 1rem;
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.08);
  }
  .geo__stat-label {
    margin: 0;
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--muted);
  }
  .geo__stat-value {
    margin: 0.5rem 0 0;
    font-size: 1.6rem;
    font-weight: 700;
  }
  .geo__admin {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.5rem;
    align-items: stretch;
  }
  .geo__admin-map iframe {
    width: 100%;
    min-height: 260px;
    border: 0;
    border-radius: 1.2rem;
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.08);
  }
  .geo__admin-info {
    background: #fff;
    border-radius: 1.2rem;
    padding: 1.8rem;
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.08);
  }
  .geo__admin-info h3 {
    margin: 0 0 0.6rem;
  }
  .geo__admin-info p {
    margin: 0 0 0.8rem;
    color: var(--text);
  }
  .geo__admin-info ul {
    margin: 0;
    padding-left: 1.2rem;
    color: var(--muted);
  }
  @media (max-width: 640px) {
    .geo__badge {
      width: 100%;
      text-align: center;
    }
  }
</style>
